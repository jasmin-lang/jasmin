#[ct = "secret × public -> secret"]
fn add_SP(reg u64 x, reg u64 y) -> reg u64 {
  reg u64 t;
  t = x + y;
  return t;
}

#[ct = "secret -> secret"]
fn bad_call(reg u64 x) -> reg u64 {
  x = add_SP(x, x);
  return x;
}