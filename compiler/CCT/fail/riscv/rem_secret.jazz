/* REM has data-dependent latency, it is not constant-time. */
#[ct="secret -> secret"]
export fn rem_secret(reg u32 x) -> reg u32 {
  reg u32 d q;
  x = x;
  d = 3;
  q = #REM(x, d);
  q = q;
  return q;
}
