/* Unsigned division lowers to DIVU, which is not in the Zkt safe list,
   hence not DOIT. */
#[ct="secret -> secret"]
export fn div(reg u32 x) -> reg u32 {
  reg u32 d q;
  x = x;
  d = 3;
  q = x / d;
  q = q;
  return q;
}
