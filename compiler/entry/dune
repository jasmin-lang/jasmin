(rule
 (deps (source_tree libfoo) (source_tree inputs))
 (targets libfoo.a libfoo.so dllfoo.so)
 (action
 (no-infer
  (progn
   (chdir libfoo (run make))
   (copy libfoo/libfoo.a libfoo.a)
   (copy libfoo/dllfoo.so libfoo.so)
   (copy libfoo/libfoo.so dllfoo.so)))))
