export
fn dead_stack_arg(stack u32 x, reg u32 y) -> reg u32{
  reg u32 z;
  z = y;
  return z;
}

export
fn main(reg u32 a b c d e, stack u32 f /*, reg u32 h i*/) -> reg u32{
  reg u32 g;
  a += f;
  a += b;
  a += c;
  a += d;
  a += e;
  b = a;
  return b;
} 
