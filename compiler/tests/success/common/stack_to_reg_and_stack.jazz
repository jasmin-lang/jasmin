export
fn stack_to_reg(reg u32 a b c d e f g h i j k l m n,
                stack u32 p q) -> reg u32 {
  reg u32 r;
  r = a;
  r += b;
  r += c;
  r += d;
  r += e;
  r += f;
  r += g;
  r += h;
  r += i;
  r += j;
  r += k;
  r += l;
  r += m;
  r += n;
  r += p;
  r += q;
  
  return r; 
}
