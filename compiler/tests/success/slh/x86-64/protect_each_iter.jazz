#[sct = "transient → public"]
export
fn protect_each_iter(reg u64 p) -> reg u64 {
    #[nospill] reg u64 msf;
    reg u64 temp sum i;
    reg bool b;

    msf = #init_msf();
    sum = 0;
    i = 0;

    while{b = i < 10;}(b){
        msf = #update_msf(b, msf);
        #declassify temp = [p + i];
        temp = #protect(temp, msf);
        sum += temp;
        i += 1;
    }
    msf = #update_msf(!b, msf);

    return sum;
}
