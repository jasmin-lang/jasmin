export
fn parsing_test(reg u64 s, reg u64 u) -> reg u64 {
reg u64 x;
x = s;
x = x + s;
x = x + 64;
x = x + 64s s;
x = x >> u;
x = x >>s 64;
x = x >>64s u;
return x;
}

export
fn reg32_test(reg u32 x) -> reg u32 {
reg u32 y;
y = x;
y += x;
return y;
}
