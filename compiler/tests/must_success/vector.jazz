export
fn test_mem128(reg u64 p) {
reg u128 r;

r = (u128)[p + 16 * 0];
(u128)[p + 16 * 1] = r;
}

export
fn test_xor (reg u64 p) {
reg u128 r, s;
r = (u128)[p + 16 * 0];
s = (u128)[p + 16 * 1];

r ^= s;

(u128)[p + 16 * 1] = r;

}
