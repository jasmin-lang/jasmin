param n2 : u64;

fn foo() -> stack u64 {
  reg u : u64;
  u = 0;
  return u;
}

extern fn bar() -> stack u64[n*2] {
  reg u : u64[n*2];

  // u<0..1> = foo();
  return u;
/*
ERROR: parameter n undefined
*/
}
