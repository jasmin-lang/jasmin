param n2 : u64;

fn foo() -> stack u64 {
  reg u : u64;
  reg w : u64[n];
  u = 0;
  return u;
}

extern fn bar() -> stack u64[n*2] {
  reg u : u64[n*2];

  return u;
/*
ERROR: parameter n not declared
*/
}
