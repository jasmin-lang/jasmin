param n : u64;

fn foo() -> u64 {
  reg u : u64;
  u = 0;
  return u;
}

extern fn bar() -> u64<n*2> {
  reg u : u64<n*2>;

  u[0..1] = foo();
  return u[0..n*2];
/*
ERROR: 12:9 error: type mismatch (got u64<1>, expected u64)
*/
}
