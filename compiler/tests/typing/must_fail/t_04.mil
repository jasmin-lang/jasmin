param n : u64;

fn foo() -> stack u64 {
  reg u : u64;
  reg w : u64[n];
  u = 0;
  return u;
}

extern fn bar() {
  reg u : u64[n*2];

  u = foo(u);
/*
ERROR: wrong number of arguments (got 1, exp. 0)
*/
}
