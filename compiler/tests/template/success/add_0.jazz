// fails at conversion OCaml -> Rocq due to 0 not a positive

fn f<:p>(reg ptr u64[p] r) -> reg u64 {
  reg u64 res = r [0];
  return res;
}

export fn main<:p> (reg ptr u64[p+0] r) -> reg u64 {
  reg u64 res = f<:p>(r);
  return res;
}

