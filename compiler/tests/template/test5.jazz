// we test template function instantiated on a variable

fn f<|n|>(reg ptr u64[n] r) -> reg u64 {
  reg u64 res = r[0];
  return res;
}

export fn main<|M,Q|> (reg ptr u64[M+Q] r) -> reg u64 {
  reg u64 res = f<|M+Q|>(r);
  return res;
}
