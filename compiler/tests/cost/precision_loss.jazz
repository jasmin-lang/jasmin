param int N = 4;

export
fn main(reg u64 x) -> reg u64 {
  inline int i;
  reg u64 r j;

  r = x;

  j = 42;

  while (j > 0) {
    for i = 0 to N {
      r += 1;
      if j == i {
        r -= 2;
      }
    }
    j -= 1;
  }

  return r;
}
