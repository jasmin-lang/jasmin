merging basic blocks in all function(s)
   REMOVED_TIME
>> input:
#[macro_use] extern crate jasmin;
fn foo1(mut x: stack! (b64), mut y: stack! (b64), mut z: reg! (b1)) -> stack! (b64) {
                                      var! {
                                          w: stack! (b64);
                                      }
                                      code! {
                                          // STARTBLOCK
                                          w: stack! (b64) = x: stack! (b64);
                                          w: stack! (b64) += x: stack! (b64);
                                          // ENDBLOCK
                                          // STARTBLOCK
                                          if (w: stack! (b64) == jc!(5)) {
                                              // STARTBLOCK
                                              z: reg! (b1), x: stack! (b64) += w: stack! (b64);
                                              z: reg! (b1), x: stack! (b64) += y: stack! (b64);
                                              // ENDBLOCK
                                          }
                                          // ENDBLOCK
                                      }
                                      return x: stack! (b64)
                                  }


total transformation time: REMOVED_TIME

total time (with save/print): REMOVED_TIME
