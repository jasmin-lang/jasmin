merging basic blocks in all function(s)
   REMOVED_TIME
>> input:
fn foo1(x : stack u64, y : stack u64, z : reg bool) -> stack u64 {
  w : stack u64
  // START
  (w : stack u64) = (x : stack u64);
  (w : stack u64) += (x : stack u64);
  // END
  // START
  if (w : stack u64 = 5) {
    // START
    (z : reg bool), (x : stack u64) += (w : stack u64);
    (z : reg bool), (x : stack u64) += (y : stack u64);
    // END
  }
  // END
  return (x : stack u64);
}


total transformation time: REMOVED_TIME

total time (with save/print): REMOVED_TIME
