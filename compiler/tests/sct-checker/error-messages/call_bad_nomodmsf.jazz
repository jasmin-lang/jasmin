#nomodmsf
fn wrong_nomodmsf(reg u64 x) -> reg u64  {
    if (x == 0) {
        x = 2;
    }
    return x;
}

export fn call_bad_nomodmsf(reg u64 x) {
    reg u64 msf;
    msf = #init_msf();
    x = wrong_nomodmsf(x);
    x = #protect(x, msf);
}
