fn modmsf_destroys(reg u64 x) -> reg u64  {
    if (x == 0) {
        x = 2;
    }
    return x;
}

export fn call_modmsf_destroys(reg u64 x) {
    reg u64 msf;
    msf = #init_msf();
    x = modmsf_destroys(x);
    x = #protect(x, msf);
}
