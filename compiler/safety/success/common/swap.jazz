export
fn swapw(reg u32 x y) -> reg u32, reg u32 {
  x = x;
  y = y;
  x, y = #swap(x, y);
  return x, y;
}

export
fn swapa(reg u32 x) -> reg u32 {
  stack u32[1] a b;
  a[0] = x;
  b[0] = x;
  a, b = #swap(a, b);
  x = a[0];
  return x;
}
