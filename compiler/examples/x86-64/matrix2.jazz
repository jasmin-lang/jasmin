fn f(reg ptr u64[1] p) -> reg ptr u64[1] {
    p[0] += 1;
    return p;
}

export
fn main() -> reg u64 {
    reg ptr u64[1] res;
    stack u64[1] s_res;
    res = s_res;

    res = f(res);

    reg u64 x;
    x = res[0];
    return x;
}
