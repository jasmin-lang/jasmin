extern fn test(x, y) {
  h, l = x * y;
  h = h + l;
  return h ,l;
}

/*

extern fn test(x, y, z) {
  h1, l1 = x * y;
  h2, l2 = z * y;
  w = h1;
  h1 = w;
  h1 = h2 + l1;
  return h1 ,l2;
}

"required register (%rax/%rdx) already in use"

extern fn mymul(x, y) {
  h, l = x * y;
  w = h;
  h = w;
  return h ,l, x;
}

"equal_dest_src: source with equality constraint still live after instruction"

*/
