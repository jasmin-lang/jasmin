export
fn memcmp(reg u64 p, reg u64 q, reg u64 n) -> reg u64 {
reg u64 r;
reg u64 i;
reg u64 a;
reg u64 b;
reg u64 pi;
reg u64 qi;
r = 1;
i = 0;
while (i < n) {
pi = p; pi += i;
qi = q; qi += i;
 a = [pi + 0];
 b = [qi + 0];
 r = 0 if a != b;
i += 1;
}
return r;
}
